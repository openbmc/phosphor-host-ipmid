
if get_option('serialbridge').allowed() or \
        get_option('transport-implementation') == 'serial'
    subdir('serialbridge')
endif
if get_option('rmcpbridge').allowed()
    subdir('rmcpbridge')
endif
